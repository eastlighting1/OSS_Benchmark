# GNN Data Backend Benchmark System

Benchmark system to evaluate data backend performance for GNN training pipelines.

## Setup
```bash
uv sync
```

## Execution
```bash
uv run main.py --dataset ogbn-arxiv --data-dir ../data/
```

See `task3.md` for more details.
